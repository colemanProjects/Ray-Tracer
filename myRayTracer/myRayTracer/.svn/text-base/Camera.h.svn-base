#pragma once
#include <vector>

using namespace std;
class Camera
{
private:
	vector<float> startLocation;
	vector<float> focalPoint;
	
public:
	double focalLength;
	Camera(void);

	vector<float> getLocation();
	vector<float> getFocalPoint();
	void setLocation(float x, float y, float z);
	void changeDirection(float x, float y, float z);
};

